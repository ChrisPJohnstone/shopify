query GetProducts {
  products(first: 10) {
    nodes {
      id
      title
      description
      priceRange {
        minVariantPrice { amount }
        maxVariantPrice { amount }
      }
    }
  }
}
