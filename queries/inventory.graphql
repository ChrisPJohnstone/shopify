fragment InventoryInfo on InventoryItem {
  id
  variant {
    # Name of product
    displayName
    # Cost to customer
    price
  }
  unitCost {
    # Cost to business
    amount
  }
  inventoryLevels(first: 50) {
    nodes {
      quantities(names: ["available"]) {
        name
        quantity
      }
    }
  }
}

query InventoryItems {
  inventoryItems(first: 5) {
    nodes {
      ...InventoryInfo
    }
  }
}
